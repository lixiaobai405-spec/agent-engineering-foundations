# Task Evidence: phase-1d-task-10

## 1. Identity

- Task ID: `phase-1d-task-10`
- Task name: 引入 React/Vite/Vitest 并建立 typed Chat state
- Authoritative plan or task spec: `docs/agent-plans/2026-08-02-phase-1d-interactive-chat-ui-plan.md` Task 10 plus user-provided executor prompt
- Evidence status: `completed-awaiting-review` (one integration test conflicts with successful build output)
- TDD required: `yes`
- Started at: `2026-08-07T16:48+08:00`
- Completed at: `2026-08-07T16:56+08:00`
- Executor role confirmed: `yes`

### Scope

- Install React/Vite/Vitest and Testing Library dependencies.
- Add Chat build/test/typecheck scripts while preserving Viewer scripts.
- Scaffold typed Chat state (`types`, `api`, `events`, `reducer`).
- Minimal `App.tsx` loading conversations via typed API client.
- Build output at `src/agent_foundations/viewer/static/chat/` with `base: /chat-static/`.

### Non-scope

- Task 11 UI components (forms, approval cards, activity panels, full chat layout).
- Backend domain contract changes.
- Task 12 E2E/docs/plan checkbox updates.
- Commit/push/PR.

## 2. Pre-change Snapshot

- Branch: `main`
- Node.js: `v24.15.0`
- npm: `11.12.1`

### Dependency authorization

- User authorization (verbatim summary): **「授权 npm view 和 npm install」** (2026-08-07)
- Authorized: `npm view`, `npm install`
- Not authorized: commit, push, PR, Task 11

### Engine compatibility (`npm view`)

```text
npm view vite engines --json
{ "node": "^20.19.0 || >=22.12.0" }

npm view vitest engines --json
{ "node": "^20.0.0 || ^22.0.0 || >=24.0.0" }
```

Current Node `v24.15.0` satisfies both. Proceeded with install.

### Install commands and results

| Command | Exit code | Output summary |
|---|---:|---|
| `npm install react react-dom` | 0 | added 3 packages in 9s |
| `npm install --save-dev @types/node @types/react @types/react-dom @vitejs/plugin-react vite vitest jsdom @testing-library/react @testing-library/dom @testing-library/user-event @testing-library/jest-dom` | 0 | added 111 packages in 29s |

Resolved direct dependency versions (`package.json`):

| Package | Version |
|---|---|
| `react` | `^19.2.8` |
| `react-dom` | `^19.2.8` |
| `vite` | `^8.2.1` |
| `vitest` | `^4.1.10` |
| `@vitejs/plugin-react` | `^6.0.5` |
| `typescript` | `^5.7.3` (existing) |
| `@testing-library/react` | `^16.3.2` |
| `@testing-library/jest-dom` | `^7.0.0` |
| `jsdom` | `^30.0.1` |

Install warnings: npm reported `26 packages are looking for funding`; no engine mismatch, no install failure. Did not run `npm audit fix` or change Node.js.

### Pre-change Task 10 file existence

All Task 10 targets absent before implementation (`web/chat/**`, `tests/chat/**`, configs).

### Existing user changes preserved

All prior modified/untracked Phase 1B/1C/1D paths retained; no reset/clean.

## 3. Red

- Recorded before production-code changes: `yes`
- Time: `2026-08-07T16:52:56+08:00`
- Test file: `tests/chat/reducer.test.ts` (11 tests defined)
- Command: `npm run test:chat`
- Exit code: `1`
- Relevant verbatim output:

```text
 FAIL  tests/chat/reducer.test.ts [ tests/chat/reducer.test.ts ]
Error: Failed to resolve import "../../web/chat/state/reducer" from "tests/chat/reducer.test.ts". Does the file exist?
 Test Files  1 failed (1)
      Tests  no tests
```

- Expected failure category: missing reducer/types implementation
- Why valid Red: config and dependencies installed; failure is missing Task 10 state module, not syntax/env/path typo in unrelated code.

## 4. Green

- Production files created:
  - `tsconfig.chat.json`, `vite.config.ts`, `vitest.config.ts`
  - `web/chat/index.html`, `main.tsx`, `App.tsx`
  - `web/chat/state/types.ts`, `api.ts`, `events.ts`, `reducer.ts`
  - `tests/chat/setup.ts`
- Modified: `package.json`, `package-lock.json`
- Generated by `build:chat`: `src/agent_foundations/viewer/static/chat/index.html`, `.../assets/index-x0yTSECM.js`
- Command: `npm run test:chat`
- Exit code: `0`
- Relevant verbatim output:

```text
 Test Files  1 passed (1)
      Tests  11 passed (11)
```

## 5. Regression and Quality Gates

| Check | Command | Exit code | Result |
|---|---|---:|---|
| Target tests | `npm run test:chat` | 0 | 11 passed |
| Chat typecheck | `npm run typecheck:chat` | 0 | pass (after reducer test TS fix) |
| Chat build | `npm run build:chat` | 0 | built to `src/agent_foundations/viewer/static/chat/`; asset URL `/chat-static/assets/index-x0yTSECM.js` |
| Viewer tests | `npm run test:viewer` | 0 | 9 passed |
| Viewer typecheck | `npm run typecheck:viewer` | 0 | pass |
| Python integration | `conda run -n agent-foundations python -m pytest tests/integration/test_chat_api.py tests/integration/test_viewer_api.py -q` | 1 | **19 passed, 1 failed** |
| `git diff --check` | `git diff --check` | 0 | pass (LF/CRLF warnings only on tracked pre-existing files) |

### Integration failure detail

```text
FAILED tests/integration/test_chat_api.py::test_chat_enabled_routes_and_missing_build_503
assert root.status_code == 503
E assert 200 == 503
```

Cause: Task 10 `build:chat` created `src/agent_foundations/viewer/static/chat/index.html`, so Chat routes correctly return `200` instead of pre-Task-10 `503 Chat UI build is missing`. This is expected post-build behavior; the test asserts the opposite precondition. No backend code was changed.

## 6. Scope Audit

### Final changed/created files (Task 10)

| Path | Purpose |
|---|---|
| `package.json` | Chat scripts + dependencies |
| `package-lock.json` | Locked dependency tree |
| `tsconfig.chat.json` | Chat TS project |
| `vite.config.ts` | Vite build (`root=web/chat`, `base=/chat-static/`) |
| `vitest.config.ts` | Vitest jsdom config |
| `web/chat/**` | Minimal React entry + typed state |
| `tests/chat/**` | Reducer tests + setup |
| `src/agent_foundations/viewer/static/chat/**` | **build output** |
| `docs/task-evidence/phase-1d-task-10.md` | This evidence |

- Unrelated production changes introduced: **no**
- Existing user changes preserved: **yes**
- Secrets or sensitive payloads in evidence/diff: **no**
- Commit/push/deployment/next Task performed: **no**
- Viewer scripts preserved: `build:viewer`, `typecheck:viewer`, `test:viewer` unchanged

## 7. Gaps and Limitations

- Python integration gate: **partial** — 1 test conflicts with successful Chat build artifact on disk.
- Plan Task 10 checkbox: **not updated** (integration gate not fully green per executor criteria).
- No `npm audit fix`, no Node downgrade, no out-of-scope file edits.

## 8. Handoff Summary

- Current verification status: **partial** (all Task 10 frontend gates green; 1 integration test fails due to build artifact presence)
- TDD process evidence: **complete** (valid Red before production code)
- Recommended reviewer commands:

```powershell
npm run test:chat
npm run typecheck:chat
npm run build:chat
npm run test:viewer
npm run typecheck:viewer
conda run -n agent-foundations python -m pytest tests/integration/test_chat_api.py tests/integration/test_viewer_api.py -q
git diff --check
```

Reviewer note: expect `test_chat_enabled_routes_and_missing_build_503` to fail when `static/chat/index.html` exists; pass when build output removed.

---

## 9. Remediation (2026-08-07 acceptance fixes)

Scope: fix three reviewer findings only. No dependency install, no Task 11, no backend changes.

### 9.1 Red — reducer selection semantics

- Time: `2026-08-07T17:18:33+08:00`
- Command: `npm run test:chat`
- Exit code: `1`
- Relevant output:

```text
4 failed | 12 passed
expected null to be '11111111-1111-4111-8111-111111111111'
```

- Cause: `conversations.loaded` did not establish `activeConversationId` per plan.

### 9.2 Red — type contract (nullable required fields)

- Time: `2026-08-07T17:18:35+08:00` (before `types.ts` fix)
- Command: `npm run typecheck:chat`
- Exit code: `2`
- Relevant output:

```text
tests/chat/reducer.test.ts(399,49): error TS2344: Type '{ assistant_message_id: string | null; ... }' does not satisfy the constraint ...
```

- Cause: `RunRecord` / `ApprovalRequest` nullable fields declared optional (`?`) instead of required `string | null`.

### 9.3 Red — integration stale test contract

- Time: `2026-08-07T17:17+08:00` (before `test_chat_api.py` fix)
- Command: `conda run -n agent-foundations python -m pytest tests/integration/test_chat_api.py tests/integration/test_viewer_api.py -q`
- Exit code: `1`
- Relevant output:

```text
FAILED test_chat_enabled_routes_and_missing_build_503
assert 200 == 503
1 failed, 19 passed
```

- Classification: stale test contract (build present on disk); not production-code Red.

### 9.4 Green — fixes applied

| File | Change |
|---|---|
| `web/chat/state/reducer.ts` | `resolveActiveConversationId()` on `conversations.loaded` |
| `web/chat/state/types.ts` | Required nullable fields (`string \| null`, not `?`) |
| `tests/chat/reducer.test.ts` | Restored plan assertion; selection + `expectTypeOf` tests |
| `tests/integration/test_chat_api.py` | Split build-present (`200`) vs build-missing (`503` via `monkeypatch`) |

### 9.5 Green and full regression gates

| Check | Command | Exit code | Result |
|---|---|---:|---|
| Chat tests | `npm run test:chat` | 0 | 16 passed |
| Chat typecheck | `npm run typecheck:chat` | 0 | pass |
| Chat build | `npm run build:chat` | 0 | `/chat-static/assets/` in output |
| Viewer tests | `npm run test:viewer` | 0 | 9 passed |
| Viewer typecheck | `npm run typecheck:viewer` | 0 | pass |
| Integration | `pytest tests/integration/test_chat_api.py tests/integration/test_viewer_api.py -q` | 0 | **21 passed**, 1 Starlette deprecation warning |
| Full pytest | `pytest -q` | 0 | **556 passed**, 1 warning |
| Whitespace | `git diff --check` | 0 | pass |
| Dependencies | `npm ls --depth=0` | 0 | unchanged tree |

### 9.6 Route verification

**Build present (real `static/chat/index.html`):**

- `GET /` → 200, `text/html`, references `/chat-static/assets/…`
- `GET /chat` → 200
- Static asset under `/chat-static/assets/` → 200
- `GET /trace` → 200

**Build missing (`monkeypatch` `CHAT_BUILD_DIR` → non-existent `tmp_path`):**

- `GET /` → 503, `{"detail":"Chat UI build is missing"}`
- `GET /chat` → 503, same body
- `GET /trace` → 200

### 9.7 Remediation scope audit

Modified paths (untracked in git):

```text
?? web/chat/state/reducer.ts
?? web/chat/state/types.ts
?? tests/chat/reducer.test.ts
?? tests/integration/test_chat_api.py
?? docs/task-evidence/phase-1d-task-10.md
?? docs/agent-plans/2026-08-02-phase-1d-interactive-chat-ui-plan.md
```

- `git diff --cached --name-only`: empty
- No commit/push; no new dependencies; user changes preserved
- Plan Task 10 Steps 1–7: **checked** after full green

### 9.8 Updated handoff

- Current verification status: **pass** (remediation gates all green)
- TDD process evidence: **complete** (reducer + type contract Red before production fix; integration classified as stale-test Red)
- Unverified: none for remediation scope

---

## 10. Remediation — Ruff I001 import order (2026-08-07)

### Reviewer finding

`conda run -n agent-foundations python -m ruff check .` returned **I001** on `tests/integration/test_chat_api.py`:

```text
I001 Import block is un-sorted or un-formatted
  --> tests\integration\test_chat_api.py:1:1
import re was after `from collections.abc import ...`; Ruff expects `import re` before `import time`.
```

### TDD

- **not-applicable** — pure stdlib import reorder; no behavior or assertion change. Existing Ruff I001 is reviewer/pre-fix gate evidence, not functional TDD Red.

### Change

| File | Change |
|---|---|
| `tests/integration/test_chat_api.py` | Move `import re` into alphabetical stdlib block (`asyncio`, `json`, `re`, `time`) |

No other files modified in this round.

### Fresh verification

| Check | Command | Exit code | Result |
|---|---|---:|---|
| Ruff | `conda run -n agent-foundations python -m ruff check .` | 0 | All checks passed |
| mypy | `conda run -n agent-foundations python -m mypy src tests` | 0 | Success: 82 source files |
| Chat tests | `npm run test:chat` | 0 | 16 passed |
| Chat typecheck | `npm run typecheck:chat` | 0 | pass |
| Chat build | `npm run build:chat` | 0 | built to `static/chat/` |
| Viewer tests | `npm run test:viewer` | 0 | 9 passed |
| Viewer typecheck | `npm run typecheck:viewer` | 0 | pass |
| Integration | `pytest tests/integration/test_chat_api.py tests/integration/test_viewer_api.py -q` | 0 | 21 passed, 1 warning |
| Full pytest | `pytest -q` | 0 | 556 passed, 1 warning |
| Dependencies | `npm ls --depth=0` | 0 | unchanged |
| Whitespace | `git diff --check` | 0 | pass (LF/CRLF warnings on pre-existing tracked files only) |

**Retained warning:** `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.` (pre-existing; not introduced by this fix).

### Scope audit

- `git diff --cached --name-only`: empty (nothing staged)
- User pre-existing changes: preserved
- Task 11 plan checkboxes: unchanged (all `[ ]`)
- Task 10 Step 7: remains `[x]` (all gates green)
- Commit/push: no

### Updated handoff (post I001 fix)

- Current verification status: **pass**
- TDD process evidence (Task 10 overall): **complete** (functional Red/Green from initial implementation + remediation; this round **not-applicable**)
- Unverified items: **none**
